# Componentes

- Reutilize componentes existentes.
- Prefira variantes e props antes de criar novos componentes.
- Exporte componentes públicos pelo `index.js`.
- Preserve APIs existentes (`variant`, `size`, `loading`, `disabled`, etc.).
